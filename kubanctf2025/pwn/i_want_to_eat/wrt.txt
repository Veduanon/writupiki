Сервис: 62.173.139.193:18002

Уязвимость: переполнение буфера в пункте меню «Бронирование столика». В book_table() читают до 0x100 байт в буфер ~0x30 → можно перезаписать сохранённый RIP.

Win-функция: make_coffee() по адресу 0x401216 вызывает execl("/bin/sh","sh",NULL) → shell.

Оффсет до RIP: 56 байт (0x30 буфер + 8 сохранённый RBP).

Пейлоад: b"A"*56 + p64(0x401216).

Шаги эксплуатации:

Отправить 1 (бронь).

На «имя» — переполняющий пейлоад.

На «номер столика» — любое число.

После ret попадаем в make_coffee(), получаем /bin/sh и читаем флаг: cat flag.txt.